server:
  port: 80
spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#消费者将要去访问微服务名称（成功注册仅nacos的服务提供者）
service-url:
    nacos-user-service: http://nacos-provider